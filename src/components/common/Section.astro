---
interface Props {
  id: string;
  title?: string;
  class?: string;
}

const { id, title, class: className } = Astro.props;
---

<section id={id} data-section={id} class={className}>
  {
    title && (
      <div class="mb-5 flex gap-3 text-xl font-semibold">
        <span>▼</span>
        <h2 class="font-mono">{title.toUpperCase()}</h2>
      </div>
    )
  }
  <slot />
</section>
