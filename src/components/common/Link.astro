---
import { cn } from '@/lib/utils';

interface Props {
  href: string;
  title: string;
  newTab?: boolean;
  styled?: boolean;
  class?: string;
}

const {
  href,
  title,
  newTab = true,
  styled = true,
  class: className
} = Astro.props;
---

<a
  href={href}
  title={title}
  aria-label={title}
  target={newTab ? '_blank' : '_self'}
  rel="noopener noreferrer"
  class={cn(styled ? 'underline hover:text-muted-foreground' : '', className)}
  ><slot /></a
>
