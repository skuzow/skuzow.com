---
import '@/styles/globals.css';
import { ViewTransitions } from 'astro:transitions';

import Header from '@/components/layout/Header.astro';
import Footer from '@/components/layout/Footer.astro';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  // image?: string;
}

const {
  title,
  description,
  canonical = ''
  // image = '/logo.avif'
} = Astro.props;
---

<!doctype html>
<html lang="en" class="scroll-smooth motion-safe:scroll-auto">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>

    <link
      rel="preload"
      href="/fonts/GeistVF.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />
    <link
      rel="preload"
      href="/fonts/GeistMonoVF.woff2"
      as="font"
      type="font/woff2"
      crossorigin
    />

    <meta name="description" content={description} />
    <meta
      name="keywords"
      content="Alejandro Porras, skuzow, portfolio, blog, minimalist"
    />
    <meta name="author" content="Alejandro Porras - skuzow" />
    <link rel="canonical" href={new URL(canonical, Astro.url)} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:site" content="@skuzow" />
    <meta name="twitter:creator" content="@skuzow" />
    <!-- <meta name="twitter:image" content={new URL(image, Astro.url)} />
    <meta name="twitter:image:alt" content={title} /> -->

    <meta property="og:locale" content="en_EN" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <!-- <meta property="og:image" content={new URL(image, Astro.url)} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" /> -->
    <meta property="og:site_name" content={title} />
    <meta property="og:url" content={new URL(canonical, Astro.url)} />

    <meta name="robots" content="index, follow" />
    <meta name="googlebot" content="index, follow" />

    <meta name="generator" content={Astro.generator} />
    <ViewTransitions />
  </head>
  <body class="relative flex min-h-screen flex-col gap-2 py-4 md:py-6">
    <Header />
    <main
      class="container flex max-w-4xl flex-1 flex-col gap-10 py-4 duration-500 animate-in fade-in-20 slide-in-from-bottom-2"
    >
      <slot />
    </main>
    <Footer />

    <style is:global>
      :root {
        color-scheme: light dark;
      }
    </style>
  </body>
</html>
